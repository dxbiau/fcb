# ═══════════════════════════════════════════════════════════════
# FCB Strategy Backtester — Dependencies
# ═══════════════════════════════════════════════════════════════
# Install: pip install -r requirements.txt
# Or:      pip install -e .            (editable / development)
# Or:      pip install -e ".[dev]"     (with dev tools)
# ═══════════════════════════════════════════════════════════════

# ─── Core ───
pandas>=2.0
numpy>=1.24

# ─── Exchange Data ───
ccxt>=4.0

# ─── Analysis & Visualization ───
matplotlib>=3.7
seaborn>=0.12
tabulate>=0.9

# ─── Testing ───
pytest>=7.0
pytest-cov>=4.0

# ─── Freqtrade (optional — only for live/paper trading) ───
# pip install freqtrade

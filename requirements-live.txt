# ═══════════════════════════════════════════════════════════════
# FCB Bot — Live Trading Dependencies (minimal)
# ═══════════════════════════════════════════════════════════════
# Only what the live bot needs. No matplotlib, no pytest, no
# backtest/analysis deps. Keeps the container small and fast.
# ═══════════════════════════════════════════════════════════════

# Exchange connectivity
ccxt>=4.0

# Data handling (used by strategy signal logic)
pandas>=2.0
numpy>=1.24

# Table formatting for logs
tabulate>=0.9
